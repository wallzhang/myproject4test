1.首先明确一点要做简化ioc的话，其实我这边主要是要写出content类，类内维护一个ioc容器，而容器其实相当于一个map。
我这边需要做的事情如下
    1）扫描包路径,获取包路径下的所有类
    2）遍历类，拿到带有我自定义注解的类将他注册到ioc容器内，key为类名首字母小写，value为通过反射获取的类实例。
    3）想要实现的注解主要有(@WallCompent)
    4) 测试的时候我希望能从content中拿到@Compent注解的类实例,然后通过反射调用对应的方法
 2.补上DI功能，DI做了以下操作
    1）遍历ioc容器，获取容器内的clazz,并从中找到带有@WallAutowired修饰的属性
    2）对带有注解的属性进行自动赋值
